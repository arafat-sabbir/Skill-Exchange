import { useLoaderData } from "react-router-dom";

const BidJob = () => {
    const JobDetail = useLoaderData()
    return (
        <div>
            
        </div>
    );
};

export default BidJob;